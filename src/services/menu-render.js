export const render = (menuOptions, hovered) => {
  for (let i = 0; i < menuOptions.length(); i++){
    if i == hovered{
      console.log(menuOptions[i]);
    }
    else{
      console.log(menuOptions[i]);
    }
  }
}
